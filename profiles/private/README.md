Keep company buyers out of git. This folder is a second profiles root.

```
profiles/private/packs/<pack-id>/pack.yaml
profiles/private/packs/<pack-id>/*.yaml
```

It loads next to `profiles/packs` when the directory exists. The public repo ships example buyers only.
